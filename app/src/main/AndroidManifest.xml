<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acherniakovich.android.astrology">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="locale"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectLenguage" />
        <activity
            android:name=".AddInformation"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Prognoz" />
        <activity android:name=".Sovmestimost" />
        <activity android:name=".SovmResult" />
        <activity android:name=".Periodi"></activity>
    </application>

</manifest>